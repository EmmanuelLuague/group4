export const auth = () => {
    return (
        <div classname="App">
            <input placeholder="Email..." />
            <input placeholder="Password..." />
            <button>Sign in</button>
        </div>
    );
};

export default auth;